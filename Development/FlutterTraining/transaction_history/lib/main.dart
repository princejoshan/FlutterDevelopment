import 'package:flutter/material.dart';

void main() {
  runApp(const MyTransaction());
}

class MyTransaction  extends StatelessWidget {
  const MyTransaction({Key? key}) : super(key: key);


  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}